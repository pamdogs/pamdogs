1) Clone repo
2) composer install
3) create .env (on root folder, from .env.example) and create database 
4) php artisan key:generate
5) php artisan migrate

# Quizas no sea necesario
6) check config/app.php -> 'url'
